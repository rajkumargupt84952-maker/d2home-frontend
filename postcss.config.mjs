export default {
    theme: {
        extend: {
            fontFamily: {
                sans: ['Poppins', 'sans-serif'], // globally override default sans
            },
        },
    },
    plugins: {
        '@tailwindcss/postcss': {},
    },
};
